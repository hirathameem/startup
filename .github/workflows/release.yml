name: release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: "Release"
    runs-on: "ubuntu-latest"
    steps:
      - uses: "marvinpinto/action-automatic-release@latest"
        with:
          repo_token: "${{ secrets.G_TOKEN }}"
          prerelease: false





